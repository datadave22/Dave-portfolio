## Packages
framer-motion | Complex page transitions and scroll-triggered animations
date-fns | Formatting dates for blog posts and projects
react-hook-form | Form handling for contact page
@hookform/resolvers | Zod resolver for forms

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Space Grotesk", "sans-serif"],
}
API uses strict Zod validation. Dates are returned as strings in JSON.
